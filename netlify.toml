[build]
  command = "npm install --legacy-peer-deps && npx expo build:web"
  publish = "web-build"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200